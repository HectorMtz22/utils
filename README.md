# utils

A multi-project repository. Each top-level directory is a self-contained Python project with its own `pyproject.toml` and dependencies.

## Projects

- [`pdf_crop/`](pdf_crop/) — CLI + TUI to extract page ranges from PDFs.
